VITE_API_URL=https://trading-backend.mabhatti.workers.dev
VITE_STRIPE_PUBLIC_KEY=pk_live_your_key_here
# Set to 'true' to disable Stripe loading and skip subscription API calls (but keep all code in place)
VITE_BYPASS_STRIPE=true
# Add other VITE_* variables required in production (no secrets in repo)
